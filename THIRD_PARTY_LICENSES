# Third-Party Licenses

This project began as a fork of
[chris023/orion-waveshare-rotary-dial](https://github.com/chris023/orion-waveshare-rotary-dial),
© 2026 Chris Meyer, licensed under the PolyForm Noncommercial License 1.0.0
(see [`LICENSE`](LICENSE)) — most of the codebase (board bring-up, the UI
framework, the OTA/update pipeline, the app's overall structure) is still
his original work. This fork adds substantial original work of its own on
top of that base — among it the Somnus pad API client (`dial_somnus`), pad
auto-discovery (`dial_pad_discovery`), the on-device timezone source
(`dial_time`'s IANA/POSIX handling and the paths that feed it), the
relative-temperature-scale rework (`dial_state.h`), and the connect-phase
navigation fixes in `main.c` — under the same PolyForm Noncommercial License
1.0.0 terms, © 2026 the Somnus Dial contributors. Both layers also build on
hardware bring-up code, fonts, data, and libraries from other sources, each
under its own terms. This file inventories those third-party components. The
license on this repository covers this project's original work only (both
layers above), and does not relicense the third-party files listed below.

## Rotary knob decoder — `bidi_switch_knob.c` / `.h`

- **What:** Quadrature decoder driving the rotary knob input.
- **Where:** `firmware/dial-idf/components/dial_knob/`. Earlier copies existed
  in this project's now-removed prototype directories; they're preserved in
  this repo's git history.
- **Origin:** Espressif's `iot_knob` decoder (the `knob` component in
  `esp-iot-solution`), as vendored into Waveshare's `04_Encoder_Test` demo
  for this board, with a small modification credited in-file to "planevina"
  (2025-01-20).
- **License:** Apache License 2.0. SPDX header in each file:
  `SPDX-FileCopyrightText: 2016-2024 Espressif Systems (Shanghai) CO LTD` /
  `SPDX-License-Identifier: Apache-2.0`.
- **Link:** https://github.com/espressif/esp-iot-solution/tree/master/components/knob

## Waveshare demo-derived board bring-up

- **What:** I2C bus setup, backlight PWM, touch controller glue, and the
  QSPI display driver plumbing.
- **Where:** `firmware/dial-idf/components/i2c_bsp/`,
  `firmware/dial-idf/components/lcd_bl_pwm_bsp/`,
  `firmware/dial-idf/components/lcd_touch_bsp/`, and parts of
  `firmware/dial-idf/components/dial_display/`.
- **Origin:** Adapted from Waveshare's official, publicly distributed
  `08_LVGL_Test` / `04_Encoder_Test` ESP-IDF demo for the
  ESP32-S3-Knob-Touch-LCD-1.8 board (see the Provenance section of
  `firmware/dial-idf/README.md`). These files carry no SPDX or copyright
  header from Waveshare — that's the vendor's practice for this demo code,
  not an omission on our part.
- **License:** None stated by Waveshare. Provenance is disclosed here for
  transparency: this is vendor hardware-support code, © Waveshare, included
  because it's required to run this board's display/touch/I2C peripherals.
  It is not this project's original work, and this repository's license
  does not purport to relicense it.
- **Link:** https://www.waveshare.com/wiki/ESP32-S3-Knob-Touch-LCD-1.8

## Numeral font — `dial_font_num_88.c`

- **What:** Generated LVGL bitmap font used for the large temperature/time
  numerals on the dial face.
- **Where:** `firmware/dial-idf/components/dial_ui/dial_font_num_88.c`.
- **Origin:** Generated by LVGL's `lv_font_conv` from **Montserrat-SemiBold**
  (per the file's own header: `--font Montserrat-SemiBold.ttf --size 88
  --bpp 4 ...`). Montserrat is designed by Julieta Ulanovsky and contributors.
- **License:** SIL Open Font License 1.1.
- **Link:** https://openfontlicense.org/ (typeface: https://github.com/JulietaUla/Montserrat)

## Icon fonts — `dial_font_icons_16.c`, `dial_font_icons_20.c`

- **What:** Generated LVGL bitmap fonts carrying the flame and snowflake
  glyphs originally built for the dial face's boost buttons. **Currently
  unused**: `scr_boost.c` and the boost feature it belonged to were removed
  during the Somnus port, and nothing under `dial_ui/` includes
  `dial_icons.h` or references `DIAL_ICON_FLAME`/`DIAL_ICON_SNOW`/
  `DIAL_ICON_SNOW3` today. The two files are still listed as build sources in
  `dial_ui/CMakeLists.txt`, but neither font symbol appears in a linked
  build's ELF/map — the linker drops them since nothing references them, so
  despite shipping as source, no icon glyph data currently reaches the
  compiled firmware. Kept here rather than deleted: they're still shipped
  source, and attribution doesn't depend on whether the linker happens to
  keep something.
- **Where:** `firmware/dial-idf/components/dial_ui/` (glyph macros in the
  now-unused `dial_icons.h`). Two sizes: 16px, 20px. A 26px cut also existed
  during development and was removed once both final sizes were settled.
- **Origin:** Generated by LVGL's `lv_font_conv` from **Bootstrap Icons**
  v1.11.3 (`bootstrap-icons.woff`), taking three private-use codepoints:
  `F7F6` fire, `F56D` snow, `F56F` snow3. Bootstrap Icons is by the Bootstrap
  Authors. An openly licensed set was chosen deliberately: macOS system fonts
  carry equivalent glyphs, but they are not redistributable and this is a
  public repository.
- **License:** MIT.
- **Link:** https://github.com/twbs/icons

## Timezone table — `zones.csv`

- **What:** IANA-to-POSIX timezone-rule lookup table used for on-device
  local-time conversion.
- **Where:** `firmware/dial-idf/components/dial_time/zones.csv`.
- **Origin:** `nayarsystems/posix_tz_db`.
- **License:** MIT License (per that repository's `LICENSE` file).
- **Link:** https://github.com/nayarsystems/posix_tz_db

## Build-time managed components (fetched by `idf.py`, not vendored)

Declared in `firmware/dial-idf/dependencies.lock` and pulled from the
Espressif Component Registry / upstream at build time — listed here for
transparency, not shipped as source in this repository:

| Component | Version (locked) | License |
|---|---|---|
| `lvgl/lvgl` | 8.4.0 | MIT |
| `espressif/cjson` (cJSON) | 1.7.19 | MIT |
| `espressif/esp_lcd_sh8601` | 2.0.1 | Apache-2.0 |
| `espressif/cmake_utilities` | 0.5.3 | Apache-2.0 |

## Image writer — `stb_image_write.h`

- **What:** Single-file PNG encoder used to write the screenshot images the
  desktop UI simulator produces.
- **Where:** `simulator/vendor/stb_image_write.h`. Used only by the desktop
  simulator build — it is not compiled into, or shipped in, the dial's own
  firmware.
- **Origin:** Sean Barrett's `stb` single-file public-domain libraries.
- **License:** Dual-licensed, public domain **or** MIT, at the user's
  option (per the license block in the file itself).
- **Link:** https://github.com/nothings/stb

## `trust_roots.pem`

- **What:** A curated bundle of 18 public CA root certificates, embedded for
  `dial_ota`'s on-device TLS verification of `api.github.com` and
  `objects.githubusercontent.com` (GitHub's release-asset CDN) — see
  `firmware/dial-idf/components/dial_ota/trust_roots.pem`.
- **Note:** These are public root certificates published by their issuing
  certificate authorities for exactly this purpose (verifying TLS chains).
  Redistributing them for that purpose raises no license concern. The bundle
  originally also verified the upstream project's cloud endpoint for its
  OAuth/MCP client, and lived under a different component. That pipeline was
  removed in the Somnus port — `dial_somnus` talks plain local HTTP, no TLS
  at all — so `dial_ota` is now this bundle's only consumer. Some included
  anchors are kept as valid roots for other well-known chains rather than
  pruned just because their original reason for inclusion no longer applies;
  see the file's own header comment for the full per-cert rationale.
